# Docker Compose Environment Variables
# This file is used by docker-compose.yml
# Copy this file to .env and update with your actual values

# ====================================
# PostgreSQL Database Configuration
# ====================================
# IMPORTANT: Change these credentials for production!
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=secure_print

# ====================================
# Backend Security Secrets
# ====================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# JWT Secret (minimum 64 characters hex)
JWT_SECRET=CHANGE_ME_GENERATE_STRONG_SECRET_64_CHARS

# JWT Refresh Secret (minimum 64 characters hex)
JWT_REFRESH_SECRET=CHANGE_ME_GENERATE_STRONG_SECRET_64_CHARS

# Encryption Key (minimum 64 characters hex)
ENCRYPTION_KEY=CHANGE_ME_GENERATE_STRONG_SECRET_64_CHARS

# ====================================
# CORS Configuration
# ====================================
# Comma-separated list of allowed origins
# For development:
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# For production with ngrok:
# CORS_ORIGIN=https://your-app.ngrok.io

# For production with domain:
# CORS_ORIGIN=https://app.example.com,https://admin.example.com

# Allow credentials (cookies, authorization headers)
CORS_CREDENTIALS=true

# ====================================
# Instructions
# ====================================
# 1. Copy this file: cp docker.env.example .env
# 2. Generate secure secrets:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 3. Update POSTGRES_PASSWORD with a strong password
# 4. Update JWT_SECRET, JWT_REFRESH_SECRET, and ENCRYPTION_KEY
# 5. Update CORS_ORIGIN for your deployment environment
# 6. NEVER commit the .env file to version control!
# 7. For production: Use CI/CD secrets or cloud secret managers
