# Google OAuth Configuration
# Get these from the Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Redirect URI must match what is configured in Google Cloud Console
# For local development, this is usually:
GOOGLE_REDIRECT_URI=http://127.0.0.1:5000/api/owners/google/callback

# Optional: Flask Secret Key (if used for sessions)
SECRET_KEY=dev_secret_key_change_in_production
